import { expect } from "chai";

describe("utils", () => {
  /*
  describe("getPatternByMessage()", () => {
    messagePatternPairs.forEach(pair => {
      it(`should return pattern with id=${pair.pattern_id} for "${pair.message}`, () => {
        const pattern = getPatternByMessage(pair.message);

        if (pair.pattern_id) {
          expect(pattern && pattern.id).to.eql(pair.pattern_id);
        } else {
          expect(pattern).to.be.undefined;
        }
      });
    });
  });
  */
});
